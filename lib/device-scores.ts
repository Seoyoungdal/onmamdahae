export type DeviceScore={songId:number;serviceId:number;title:string;kind:"original"|"annotated";blob:Blob;savedAt:number};
const DB_NAME="onmaeum-device-scores",STORE="scores";
function openDb(){return new Promise<IDBDatabase>((resolve,reject)=>{const req=indexedDB.open(DB_NAME,1);req.onupgradeneeded=()=>{const db=req.result;if(!db.objectStoreNames.contains(STORE))db.createObjectStore(STORE,{keyPath:"songId"})};req.onsuccess=()=>resolve(req.result);req.onerror=()=>reject(req.error)})}
export async function saveDeviceScore(score:DeviceScore){const db=await openDb();await new Promise<void>((resolve,reject)=>{const tx=db.transaction(STORE,"readwrite");tx.objectStore(STORE).put(score);tx.oncomplete=()=>resolve();tx.onerror=()=>reject(tx.error)});db.close()}
export async function getDeviceScores(serviceId:number){const db=await openDb();const all=await new Promise<DeviceScore[]>((resolve,reject)=>{const req=db.transaction(STORE).objectStore(STORE).getAll();req.onsuccess=()=>resolve(req.result);req.onerror=()=>reject(req.error)});db.close();return all.filter(s=>s.serviceId===serviceId)}
export async function deleteDeviceScore(songId:number){const db=await openDb();await new Promise<void>((resolve,reject)=>{const tx=db.transaction(STORE,"readwrite");tx.objectStore(STORE).delete(songId);tx.oncomplete=()=>resolve();tx.onerror=()=>reject(tx.error)});db.close()}
export async function deleteServiceScores(serviceId:number){const scores=await getDeviceScores(serviceId);const db=await openDb();await new Promise<void>((resolve,reject)=>{const tx=db.transaction(STORE,"readwrite"),store=tx.objectStore(STORE);scores.forEach(score=>store.delete(score.songId));tx.oncomplete=()=>resolve();tx.onerror=()=>reject(tx.error)});db.close();scores.forEach(score=>{localStorage.removeItem(`draw-${score.songId}`);localStorage.removeItem(`note-${score.songId}`)})}
