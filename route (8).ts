import {bindings,canEditService,userFrom} from "../../../../../lib/server-data";
export async function POST(request:Request,{params}:{params:Promise<{id:string}>}){try{const user=await userFrom(request),id=Number((await params).id),db=bindings().DB,song=await db.prepare("SELECT service_id,version FROM songs WHERE id=?").bind(id).first<{service_id:number;version:number}>();if(!song||!await canEditService(user,song.service_id))return new Response("지정 인도자만 공유 필기를 저장할 수 있습니다.",{status:403});const form=await request.formData(),file=form.get("file");if(!(file instanceof File)||file.type!=="image/png")return new Response("PNG 필기본이 필요합니다.",{status:400});const version=song.version+1,key=`scores/${song.service_id}/${id}/annotated-v${version}.png`,name=`annotated-v${version}.png`;await bindings().FILES.put(key,file.stream(),{httpMetadata:{contentType:"image/png"}});await db.prepare("UPDATE songs SET annotated_key=?,annotated_name=?,version=?,updated_at=CURRENT_TIMESTAMP WHERE id=?").bind(key,name,version,id).run();return Response.json({annotated_name:name,version});}catch(e){if(e instanceof Response)return e;return new Response("공유 필기를 저장하지 못했습니다.",{status:500})}}
